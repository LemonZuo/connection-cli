# Docker credentials (用于 Docker 构建和推送)
DOCKER_USERNAME=zuokaiqi
DOCKER_PASSWORD=your_password_here

# 其他构建配置
# BUILD_NUMBER=123          # 可以添加到版本信息中
# CUSTOM_VERSION=2.0.0      # 自定义版本号（会覆盖 git tag）

# 注意: 复制此文件到 build.env 并填入你的真实凭据
# Note: Copy this file to build.env and fill in your actual credentials 